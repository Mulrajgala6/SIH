# ── DAKSYNC backend dependencies ───────────────────────────────────────────
# Install the core (everything runs on this — SQLite, Bayesian slot
# recommender, built-in route optimiser, seeded offline geocoding):
#
#   pip install -r requirements.txt
#
# The extras in requirements-optional.txt are genuinely optional: each is
# imported lazily and the app falls back gracefully when it is absent. Install
# them only if you want the upgrade they provide (see that file for details).

# --- Web framework ---
fastapi>=0.115,<0.116
uvicorn[standard]>=0.30,<0.32
python-multipart>=0.0.9

# --- Data / ORM / config ---
SQLAlchemy>=2.0,<2.1
pydantic>=2.9,<3.0
pydantic-settings>=2.4,<3.0
python-dotenv>=1.0,<2.0

# --- Testing ---
pytest>=8.0,<9.0
